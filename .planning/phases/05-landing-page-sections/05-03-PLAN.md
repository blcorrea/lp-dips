---
phase: 05-landing-page-sections
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/IngredientsSection.tsx
  - src/components/FAQSection.tsx
autonomous: true
requirements: [SECT-04, SECT-07, FUNC-04]
must_haves:
  truths:
    - "The ingredients section is a split layout: 6 ingredient cards (cocoa highlighted) on the left, intro + expanded cocoa card on the right — no carousel"
    - "Each ingredient card shows its icon, name and an orange keyword badge pill (RITUAL/STAMINA/SPARK/PRESENCE/UNION/BALANCE)"
    - "The FAQ is a shadcn accordion; opening an item rotates the chevron 180deg and shows an orange left border"
    - "Only one FAQ item is open at a time"
  artifacts:
    - path: "src/components/IngredientsSection.tsx"
      provides: "Split ingredients layout replacing the carousel (SECT-04)"
      exports: ["default"]
      min_lines: 60
    - path: "src/components/FAQSection.tsx"
      provides: "FAQ rebuilt on shadcn Accordion (SECT-07)"
      exports: ["default"]
      min_lines: 30
  key_links:
    - from: "src/components/IngredientsSection.tsx"
      to: "Ingredients i18n namespace"
      via: "reuse ingredient data array + t(ingredient.*Key) verbatim, including blend_name (Aphrodisiac)"
      pattern: "Ingredients"
    - from: "src/components/FAQSection.tsx"
      to: "src/components/ui/accordion.tsx"
      via: "Accordion/AccordionItem/AccordionTrigger/AccordionContent + FAQ.q1..6 / a1..6"
      pattern: "accordion"
---

<objective>
Reskin two existing sections into their Figma redesigns using only design tokens + existing i18n copy — no new i18n keys. `IngredientsSection.tsx` (SECT-04) is rebuilt from an autoplay carousel into a static split layout (6 cards + expanded cocoa card). `FAQSection.tsx` (SECT-07) is rebuilt on the already-installed shadcn `Accordion` with an orange open-item border and rotated chevron. Both keep their existing section ids (`#ingredients`, `#faq`) and reuse their locked, already-translated copy verbatim (FUNC-04: the "Aphrodisiac"/"Ingredients" spellings are already correct in all 3 locales — verify, do not rewrite).

Purpose: SECT-04 + SECT-07 deliver the ingredients and FAQ sections in the new visual language while preserving all existing translated content.

Output: reskinned `src/components/IngredientsSection.tsx` and `src/components/FAQSection.tsx`. No message-file edits.
</objective>

<execution_context>
@C:/dev/dips/lp-dips/.claude/gsd-core/workflows/execute-plan.md
@C:/dev/dips/lp-dips/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-landing-page-sections/05-UI-SPEC.md
@.planning/phases/05-landing-page-sections/05-PATTERNS.md
@.planning/phases/05-landing-page-sections/05-CONTEXT.md
@.planning/phases/05-landing-page-sections/FIGMA-EXTRACTION.md

# Analogs (both files are their own pre-reskin analog)
@src/components/IngredientsSection.tsx
@src/components/FAQSection.tsx
@src/components/ui/accordion.tsx

<design_token_usage>
Use Phase 4 `@theme` utilities: `dips-purple-section` (ingredients + FAQ dark bg), `dips-card-ingredient`/`-border` (default cards + closed FAQ items), `dips-card-ingredient-hl`/`-hl-border` (highlighted cocoa card), `text-heading-md`/`text-subtitle-italic-sm` (ingredients header), `text-card-title-sm`/`text-card-title-lg`, `dips-text-lavender`/`dips-text-lavender-muted`, `rounded-card`/`rounded-card-lg`, `p-card-padding`. Accent = `bg-brand-orange`/`text-brand-orange`/`border-brand-orange` only (keyword badge pills, FAQ open-item border + chevron). `[var(--color-dips-*)]` arbitrary form is an equivalent fallback.
</design_token_usage>

<interfaces>
Reuse the existing ingredient data-array shape (from IngredientsSection.tsx) verbatim:
  interface Ingredient { nameKey; keywordKey; image; descKey; originsTitleKey; originsKey }
  6 entries: cocoa, maca, ginger, theanine, blend (Aphrodisiac), fenugreek — read via useTranslations("Ingredients").
Ingredient icon assets are in public/images/redesign/: ing-icon-cocoa.png, ing-icon-maca.png, ing-icon-ginger.png, ing-icon-theanine.png, ing-icon-herbal.png (this is the "blend"/Aphrodisiac icon), ing-icon-fenugreek.png.
shadcn accordion exports (src/components/ui/accordion.tsx):
  Accordion, AccordionItem, AccordionTrigger, AccordionContent
  AccordionTrigger already implements [&[data-state=open]>svg]:rotate-180 with a ChevronDown — reuse as-is.
FAQ copy: useTranslations("FAQ"), faqs = ['q1'..'q6'], answer = t(q.replace('q','a')).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: IngredientsSection.tsx — carousel → split layout (6 cards + expanded cocoa)</name>
  <files>src/components/IngredientsSection.tsx</files>
  <read_first>
    - src/components/IngredientsSection.tsx (itself: KEEP the `Ingredient` interface + `ingredients` data array + all `t(ingredient.*Key)` calls; DISCARD the carousel state — `activeIndex`, `getCardStyle`, `getRelativePosition`, autoplay `useEffect`, prev/next buttons, dot indicators)
    - .planning/phases/05-landing-page-sections/05-UI-SPEC.md section "4. Ingredients"
    - .planning/phases/05-landing-page-sections/05-PATTERNS.md section "IngredientsSection.tsx"
  </read_first>
  <action>
    Rebuild `src/components/IngredientsSection.tsx` in the same file/default export as a static split layout, discarding the carousel mechanics but keeping the `Ingredient` interface, the `ingredients` array, and every `t(ingredient.*Key)` call (`useTranslations("Ingredients")`). Root `<section id="ingredients" className="bg-dips-purple-section ... scroll-mt-[72px]">`, outer `container mx-auto px-6`. LEFT column: header "The ingredients" using `text-subtitle-italic-sm` + "Behind the experience." using `text-heading-md`, followed by the 6 ingredient cards. Each card shows the icon (44px, swap the `image` paths to `/images/redesign/ing-icon-*.png` — map cocoa→ing-icon-cocoa.png, maca→ing-icon-maca.png, ginger→ing-icon-ginger.png, theanine→ing-icon-theanine.png, blend→ing-icon-herbal.png, fenugreek→ing-icon-fenugreek.png), the ingredient name with `text-card-title-sm` (use the 18px token, NOT the Figma raw 16px), and a small uppercase keyword badge pill (`bg-brand-orange`, ~10px) reading `t(ingredient.keywordKey)` (RITUAL/STAMINA/SPARK/PRESENCE/UNION/BALANCE — already correct). The cocoa card is highlighted with fill `dips-card-ingredient-hl` / border `dips-card-ingredient-hl-border`; the other 5 use `dips-card-ingredient` / `dips-card-ingredient-border`; all `p-card-padding`, `rounded-card`. RIGHT column: intro copy (24px, `dips-text-lavender-muted`) plus ONE expanded cocoa card — title `text-card-title-lg`, description `t('cocoa_desc')`, and a sub-card "Origins & Curiosities" reading `t('cocoa_originsTitle')` + `t('cocoa_origins')` verbatim. FUNC-04: this section's copy already spells "Ingredients" and "Aphrodisiac Herbal Blend" correctly in all locales — reuse verbatim, introduce no misspelled variants in any new heading/label. Keep a `ScrollReveal` entrance wrapper for the blocks (do not mix framer-motion + ScrollReveal in this one file).
  </action>
  <verify>
    <automated>grep -q 'id="ingredients"' src/components/IngredientsSection.tsx && grep -q "scroll-mt-\[72px\]" src/components/IngredientsSection.tsx && grep -q "dips-purple-section" src/components/IngredientsSection.tsx && grep -q "dips-card-ingredient-hl" src/components/IngredientsSection.tsx && grep -q "ing-icon-herbal.png" src/components/IngredientsSection.tsx && grep -q "cocoa_originsTitle" src/components/IngredientsSection.tsx && ! grep -q "activeIndex" src/components/IngredientsSection.tsx && ! grep -qi "Igredient" src/components/IngredientsSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Root `<section id="ingredients">` on `dips-purple-section` with `scroll-mt-[72px]`; NO carousel state remains (grep `activeIndex` returns 0).
    - 6 ingredient cards render from the reused `ingredients` array; icons resolve to `/images/redesign/ing-icon-*.png` with `blend`→`ing-icon-herbal.png`.
    - Cocoa card uses `dips-card-ingredient-hl`; the other 5 use `dips-card-ingredient`; keyword pills use `bg-brand-orange`.
    - Right column renders the expanded cocoa card with `cocoa_originsTitle`/`cocoa_origins` reused verbatim.
    - No misspelled "Ingredients" variant appears in the file.
  </acceptance_criteria>
  <done>Ingredients renders as a static split layout with 6 cards + highlighted cocoa + expanded cocoa card; carousel removed; all existing i18n reused; builds cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FAQSection.tsx — rebuild on shadcn Accordion (orange open border + rotated chevron)</name>
  <files>src/components/FAQSection.tsx</files>
  <read_first>
    - src/components/FAQSection.tsx (itself: KEEP the `faqs = ['q1'..'q6']` array + `t(q)` / `t(q.replace('q','a'))` key-mapping trick; `useTranslations("FAQ")`; `id="faq"`)
    - src/components/ui/accordion.tsx (exports + the existing `[&[data-state=open]>svg]:rotate-180` chevron behavior to reuse as-is — do NOT fork or reinstall)
    - .planning/phases/05-landing-page-sections/05-UI-SPEC.md section "7. FAQs"
    - .planning/phases/05-landing-page-sections/05-PATTERNS.md section "FAQSection.tsx"
  </read_first>
  <action>
    Rebuild `src/components/FAQSection.tsx` (same default export) on the installed shadcn Accordion. Root `<section id="faq" className="bg-dips-purple-section ... scroll-mt-[72px]">` with the title in `text-heading-lg`. Import `Accordion, AccordionItem, AccordionTrigger, AccordionContent` from `@/components/ui/accordion` and render `<Accordion type="single" collapsible>` mapping the reused `faqs` array (`useTranslations("FAQ")`): each item `<AccordionItem value={q}>` with `<AccordionTrigger>{t(q)}</AccordionTrigger>` and `<AccordionContent>{t(q.replace('q','a'))}</AccordionContent>`. Closed item styling: fill `dips-card-ingredient` / border `dips-card-ingredient-border`, `rounded-card`, `p-card-padding`. Open item: add an orange left border via `data-[state=open]:border-l-4 data-[state=open]:border-brand-orange` on the `AccordionItem` (className passthrough), and answer text `dips-text-lavender`. Reuse the accordion's existing chevron rotation (`[&[data-state=open]>svg]:rotate-180`) as-is — do NOT reimplement it. Question text ~18px; if AllRoundGothic is not declared for this weight, use the existing FilsonPro bold (05-UI-SPEC.md §7 note). Keep the FAQ copy locked/unchanged.
  </action>
  <verify>
    <automated>grep -q 'id="faq"' src/components/FAQSection.tsx && grep -q "scroll-mt-\[72px\]" src/components/FAQSection.tsx && grep -q "@/components/ui/accordion" src/components/FAQSection.tsx && grep -q 'type="single"' src/components/FAQSection.tsx && grep -q "collapsible" src/components/FAQSection.tsx && grep -q "data-\[state=open\]:border-brand-orange" src/components/FAQSection.tsx && grep -Eq "q.replace\(.q.,.*.a." src/components/FAQSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Root `<section id="faq">` on `dips-purple-section` with `scroll-mt-[72px]`.
    - Uses `Accordion type="single" collapsible` from `@/components/ui/accordion` (no reinstall, no fork).
    - Renders all 6 Q&A from `FAQ.q1..q6`/`a1..a6` via the `t(q)` / `t(q.replace('q','a'))` mapping.
    - Open item shows an orange left border (`data-[state=open]:border-brand-orange`) and reuses the accordion's built-in chevron rotation.
    - FAQ copy unchanged (no message-file edits in this plan).
  </acceptance_criteria>
  <done>FAQ renders as a single-open shadcn accordion with orange open border + rotated chevron on the dark section bg; existing copy reused; builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n message → rendered DOM | Ingredients/FAQ copy interpolated into JSX text |
| accordion state → DOM | Radix client-side open/close state (no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Injection (XSS) | Ingredients/FAQ i18n text | mitigate | All copy rendered as `{t(...)}` JSX text nodes; no `dangerouslySetInnerHTML`; answer bodies are plain text |
| T-05-08 | Tampering | shadcn Accordion primitive | accept | Reused pre-installed `src/components/ui/accordion.tsx` unchanged; no new dependency, no fork |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan; `@radix-ui/react-accordion` already present (accordion.tsx imports it) |
</threat_model>

## Artifacts this phase produces

- **Reskinned components:** `IngredientsSection` (split layout) and `FAQSection` (shadcn accordion) — same paths/default exports.
- **No new i18n keys** — reuses existing `Ingredients.*` and `FAQ.q1..q6`/`a1..a6`.
- **Anchor ids (kept):** `#ingredients` (Hero "How It Works?" target), `#faq`; both gain `scroll-mt-[72px]`.
- **CSS utility usage:** `dips-purple-section`, `dips-card-ingredient(-hl)(-border)`, `text-heading-md/-lg`, `text-subtitle-italic-sm`, `text-card-title-sm/-lg`, `rounded-card`, `p-card-padding`, `border-brand-orange`.
- **Assets consumed (existing):** `ing-icon-cocoa/maca/ginger/theanine/herbal/fenugreek.png`.
- **shadcn reuse:** `Accordion`/`AccordionItem`/`AccordionTrigger`/`AccordionContent` from `src/components/ui/accordion.tsx`.

<verification>
- `npm run build` completes with no TS/ESLint errors (type-checks + lints both reskinned components even though they are not yet re-wired in `page.tsx`).
- Grep confirms the carousel state is fully removed from IngredientsSection.tsx.
- No edits to `messages/*.json` in this plan (git diff on message files is empty).
</verification>

<success_criteria>
- Ingredients delivers the 05-UI-SPEC §4 split (6 cards + highlighted cocoa + expanded card), no carousel.
- FAQ delivers the §7 single-open accordion with orange open border + rotated chevron.
- All existing translated copy reused verbatim; no message-file churn.
- Build is green.
</success_criteria>

<output>
Create `.planning/phases/05-landing-page-sections/05-03-SUMMARY.md` when done.
</output>
