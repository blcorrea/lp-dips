---
phase: 05-landing-page-sections
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ReviewsSection.tsx
  - src/components/BuySection.tsx
  - src/components/ProductPurchaseBox.tsx
autonomous: true
requirements: [SECT-05, SECT-06, FUNC-01]
must_haves:
  truths:
    - "The reviews section renders all real reviews from src/data/reviews.ts in a 3-column masonry on the #2d1a69 purple background (no lg:columns-4 step)"
    - "The bundle selector shows 3 cards (1x/2x/3x) with 2x pre-selected and a 'Most Popular' badge, a summary row, and a Buy Now button showing the total inline"
    - "Clicking Buy Now still fires the existing Stripe checkout with the real Shopify/Stripe prices — not design prices"
  artifacts:
    - path: "src/components/ReviewsSection.tsx"
      provides: "Reviews reskin: #2d1a69 bg + 3-col masonry (SECT-06)"
      exports: ["default"]
      min_lines: 120
    - path: "src/components/ProductPurchaseBox.tsx"
      provides: "Restyled bundle cards + summary + total-in-button, pricing logic intact (SECT-05/FUNC-01)"
      exports: ["default"]
      min_lines: 120
    - path: "src/components/BuySection.tsx"
      provides: "Bundle section visual wrapper with id=bundle (SECT-05)"
      exports: ["default"]
      min_lines: 60
  key_links:
    - from: "src/components/ReviewsSection.tsx"
      to: "src/data/reviews.ts"
      via: "import { reviews } — real data, never Figma placeholders"
      pattern: "@/data/reviews"
    - from: "src/components/ProductPurchaseBox.tsx"
      to: "src/components/BuyNowButton.tsx"
      via: "BuyNowButton with selectedBundle.priceId/quantity/totalPrice — checkout call untouched"
      pattern: "BuyNowButton"
    - from: "src/components/BuySection.tsx"
      to: "src/lib/shopify-product.ts"
      via: "getPurchasableDipsProduct() server fetch — pricing source, not design"
      pattern: "getPurchasableDipsProduct"
---

<objective>
Reskin the Reviews section (SECT-06) and the Bundle selector (SECT-05/FUNC-01) to their Figma redesigns — VISUAL ONLY. Reviews becomes a 3-column masonry on the `#2d1a69` background using the real review data unchanged. The bundle selector gets the new card/summary/button styling and folds the total into the Buy Now button row, while the pricing architecture (`BUNDLES`, real Stripe `priceId`s, default `2x`, `BuyNowButton` checkout call) stays byte-for-byte intact — this is the FUNC-01 guarantee that checkout still works with real Shopify/Stripe prices, not the Figma's wrong design prices.

Purpose: SECT-06 + SECT-05 deliver the reviews and bundle sections in the new look without touching data sources or checkout logic.

Output: reskinned `src/components/ReviewsSection.tsx`, `src/components/BuySection.tsx` (id `#buy`→`#bundle`), and a visually-restructured `src/components/ProductPurchaseBox.tsx`. `BuyNowButton.tsx` is NOT modified. No message-file edits.
</objective>

<execution_context>
@C:/dev/dips/lp-dips/.claude/gsd-core/workflows/execute-plan.md
@C:/dev/dips/lp-dips/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-landing-page-sections/05-UI-SPEC.md
@.planning/phases/05-landing-page-sections/05-PATTERNS.md
@.planning/phases/05-landing-page-sections/05-CONTEXT.md
@.planning/phases/05-landing-page-sections/FIGMA-EXTRACTION.md

# Analogs / locked-logic sources
@src/components/ReviewsSection.tsx
@src/components/BuySection.tsx
@src/components/ProductPurchaseBox.tsx
@src/components/BuyNowButton.tsx

<design_token_usage>
Use Phase 4 `@theme` utilities: `dips-purple-reviews` (#2d1a69 reviews bg), `dips-card-review`/`-border` (review cards), `rounded-card-lg` (18px), `p-card-padding` (25px), `text-heading-lg`/`text-body-lg` (reviews title/subtitle); `dips-cream` (bundle light panel), `dips-bundle-light`/`-border` (unselected cards), `dips-card-ingredient-hl` + `border-brand-orange` (selected 2x card), `dips-bundle-summary` (summary row), `dips-text-purple-deep` (card text). Accent = `bg-brand-orange`/`border-brand-orange` only ("Most Popular" badge, selected border, Buy Now). `[var(--color-dips-*)]` arbitrary form is an equivalent fallback.
</design_token_usage>

<interfaces>
LOCKED pricing/checkout contract — do NOT change values or call shape:
  BUNDLES (ProductPurchaseBox.tsx): 1x priceId price_1TZZWfGwnxe7ZLlEHKAy4rkm $29.99;
    2x priceId price_1TZZJnGwnxe7ZLlElHcai2IT unit 27.89 total 55.78 "Save 7%";
    3x priceId price_1TZZLGGwnxe7ZLlEp1x6vZYb unit 25.19 total 75.57 "Save 16% + Free Shipping".
  useState<'1x'|'2x'|'3x'>('2x')  // default pre-selection stays 2x
  BuyNowButton props consumed: priceId, quantity, label, className, productId, productName, productPrice, currency, locale.
BuyNowButton.tsx (DO NOT EDIT): POSTs { quantity, locale, attribution, priceId } to /api/stripe/create-checkout-session and redirects to data.url.
BuySection.tsx server contract (keep): getTranslations({locale, namespace:"BuySection"}) + getPurchasableDipsProduct(); passes priceAmount/currencyCode/productId/productName/locale into ProductPurchaseBox.
ReviewsSection data (keep): import { reviews, type Review } from '@/data/reviews' — 12 real reviews; Stars/ReviewCard/MediaBlock/initials/avatarColor helpers stay.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ReviewsSection.tsx — #2d1a69 bg + 3-col masonry + card restyle</name>
  <files>src/components/ReviewsSection.tsx</files>
  <read_first>
    - src/components/ReviewsSection.tsx (itself: `Stars`, `ReviewCard`, `MediaBlock`, `initials`, `avatarColor` are UNCHANGED; `import { reviews, type Review } from '@/data/reviews'` stays; only the section bg, the masonry column class, the card fill/border/radius/padding, and the section id change)
    - .planning/phases/05-landing-page-sections/05-UI-SPEC.md section "6. Reviews"
    - .planning/phases/05-landing-page-sections/05-PATTERNS.md section "ReviewsSection.tsx"
  </read_first>
  <action>
    Reskin `src/components/ReviewsSection.tsx` with minimal, targeted changes only. (a) Section root: replace `bg-gradient-to-b from-[#3b1c5a] to-brand-purple` with a flat `bg-dips-purple-reviews`; add `id="reviews"` and `scroll-mt-[72px]` to the section (it currently has no id). (b) Masonry: change the grid class `columns-2 md:columns-3 lg:columns-4` to `columns-2 md:columns-3` (drop the `lg:columns-4` step so it is 3 columns from `md` up, matching the roadmap Success Criterion 3 and Figma). (c) `ReviewCard` container: change `bg-white/[0.07] border border-white/10` to fill `dips-card-review` / border `dips-card-review-border`, radius `rounded-card-lg` (18px), padding `p-card-padding` (25px). Keep the `break-inside-avoid mb-3` masonry behavior and the hover transition. (d) Optionally align the title to the `text-heading-lg` token. Do NOT change the `reviews` data source, the `Stars`/avatar/initials logic, the quote/author markup, or the card's media block. Never substitute the Figma's 6 placeholder reviews.
  </action>
  <verify>
    <automated>grep -q "dips-purple-reviews" src/components/ReviewsSection.tsx && grep -q "columns-2 md:columns-3" src/components/ReviewsSection.tsx && ! grep -q "lg:columns-4" src/components/ReviewsSection.tsx && grep -q "dips-card-review" src/components/ReviewsSection.tsx && grep -q "@/data/reviews" src/components/ReviewsSection.tsx && grep -q 'id="reviews"' src/components/ReviewsSection.tsx && grep -q "scroll-mt-\[72px\]" src/components/ReviewsSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Section background is flat `dips-purple-reviews` (#2d1a69); the old `from-[#3b1c5a]` gradient is gone.
    - Masonry is `columns-2 md:columns-3` with NO `lg:columns-4` step.
    - `ReviewCard` uses `dips-card-review`/`dips-card-review-border`, `rounded-card-lg`, `p-card-padding`.
    - `import { reviews, type Review } from '@/data/reviews'` still present; Stars/avatar/initials logic unchanged.
    - Section has `id="reviews"` + `scroll-mt-[72px]`.
  </acceptance_criteria>
  <done>Reviews renders all real reviews in a 3-col masonry on #2d1a69 with restyled cards; data + card logic untouched; builds cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bundle selector reskin — BuySection wrapper (#bundle) + ProductPurchaseBox cards/summary/total-in-button (pricing locked)</name>
  <files>src/components/BuySection.tsx, src/components/ProductPurchaseBox.tsx</files>
  <read_first>
    - src/components/BuySection.tsx (server component: KEEP `getTranslations`/`getPurchasableDipsProduct` + the props passed to ProductPurchaseBox; change the visual wrapper + section id `buy`→`bundle`)
    - src/components/ProductPurchaseBox.tsx (KEEP `BUNDLES`, the 3 real `priceId`s, `useState(...'2x')`, `selectedBundle`, and the `BuyNowButton` element + all its props; restyle the card/summary/button JSX only)
    - src/components/BuyNowButton.tsx (DO NOT EDIT — read only to confirm the props contract you must keep passing)
    - .planning/phases/05-landing-page-sections/05-UI-SPEC.md section "5. Bundle Selector" (incl. the locked pricing note)
    - .planning/phases/05-landing-page-sections/05-PATTERNS.md section "BuySection.tsx"
  </read_first>
  <action>
    Restyle the bundle selector to 05-UI-SPEC §5 while preserving all pricing/checkout logic. In `BuySection.tsx`: change the section `id="buy"` to `id="bundle"` and its `scroll-mt-28` to `scroll-mt-[72px]`; restyle the wrapper toward the Figma split — a light `dips-cream` panel holding the bundle cards (left) beside a photo panel using `/images/redesign/experience-couple-photo.png` with a "Bring the Experience home." side heading (`text-heading-side`, right-aligned) + 3 mini badges (reuse the `dips-card-lavender` badge family). Keep the server-component shape and the exact props passed into `ProductPurchaseBox` unchanged. In `ProductPurchaseBox.tsx`: keep the `BUNDLES` array (all 3 real `priceId`s, unit/total prices, badges), the `useState<'1x'|'2x'|'3x'>('2x')` default, `selectedBundle`, and the `BuyNowButton` element with every existing prop (`priceId`, `quantity`, `label`, `productId`, `productName`, `productPrice`, `currency`, `locale`). Change ONLY the render/JSX styling: unselected 1x/3x cards fill `dips-bundle-light` / border `dips-bundle-light-border`, text `dips-text-purple-deep`; the selected (2x default) card fill `dips-card-ingredient-hl` / border `border-brand-orange` 2px with a floating "Most Popular" badge (`bg-brand-orange`, copy "Most Popular"); each card shows the `product-box-small.png` thumbnail, the bundle label, unit price, and the discount badge pill (2x/3x only, existing copy "Save 7%" / "Save 16% + Free Shipping" — never the Figma flat-price copy); the summary row ("Unit Price"/"Shipping") fill `dips-bundle-summary`. Fold the computed Total INTO the Buy Now button row: render `BuyNowButton` full-width with `justify-between` so the label sits left and the total sits right within the same pill (a layout change from today's separate summary panel + centered button) — do this by composing the label/total into the `label` prop or the button's children styling WITHOUT changing `BuyNowButton`'s `onClick`/checkout call. Use `bg-brand-orange` for the button (the normalized CTA orange — never the Figma raw `#ff6b01`).
  </action>
  <verify>
    <automated>grep -q 'id="bundle"' src/components/BuySection.tsx && ! grep -q 'id="buy"' src/components/BuySection.tsx && grep -q "getPurchasableDipsProduct" src/components/BuySection.tsx && grep -q "price_1TZZWfGwnxe7ZLlEHKAy4rkm" src/components/ProductPurchaseBox.tsx && grep -q "price_1TZZJnGwnxe7ZLlElHcai2IT" src/components/ProductPurchaseBox.tsx && grep -q "price_1TZZLGGwnxe7ZLlEp1x6vZYb" src/components/ProductPurchaseBox.tsx && grep -q "useState<'1x' | '2x' | '3x'>('2x')" src/components/ProductPurchaseBox.tsx && grep -q "BuyNowButton" src/components/ProductPurchaseBox.tsx && grep -q "dips-bundle-summary" src/components/ProductPurchaseBox.tsx && grep -q "border-brand-orange" src/components/ProductPurchaseBox.tsx && git diff --quiet src/components/BuyNowButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `BuySection.tsx` section id is `bundle` (not `buy`) with `scroll-mt-[72px]`; still a server component fetching `getPurchasableDipsProduct()` and passing the same props to `ProductPurchaseBox`.
    - `ProductPurchaseBox.tsx` still contains all 3 real Stripe `priceId`s, the `'2x'` default selection, and the `BuyNowButton` element with its full prop set.
    - Selected 2x card uses `border-brand-orange` + `dips-card-ingredient-hl` and shows a "Most Popular" badge; unselected cards use `dips-bundle-light`; summary row uses `dips-bundle-summary`.
    - The Total is rendered inside the Buy Now button row (`justify-between`); button uses `bg-brand-orange`.
    - `src/components/BuyNowButton.tsx` has ZERO diff (`git diff --quiet` passes) — FUNC-01 checkout call untouched.
  </acceptance_criteria>
  <done>Bundle selector shows the redesigned 3-card layout (2x default + Most Popular), restyled summary, and total-in-button; all pricing/priceIds/checkout logic intact; BuyNowButton untouched; builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/stripe/create-checkout-session | BuyNowButton POSTs bundle selection to create a Stripe session (untrusted input crosses here) |
| static review data → DOM | src/data/reviews.ts rendered into masonry cards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | Bundle selection → checkout request | mitigate | `priceId`/`quantity` are selected from the fixed server-known `BUNDLES` set; the checkout API/Stripe re-validates the priceId server-side; this reskin does not alter the request body or add client-controlled price/amount fields. Verified by `git diff --quiet src/components/BuyNowButton.tsx`. |
| T-05-10 | Tampering | Price integrity (design vs real) | mitigate | Real prices come from Stripe `priceId`s + `getPurchasableDipsProduct()`; Figma design prices are explicitly NOT used (FUNC-01, REQUIREMENTS Out of Scope). Visual-only change. |
| T-05-11 | Injection (XSS) | review quote/author text | mitigate | Rendered as `{review.quote}`/`{review.name}` JSX text nodes; no `dangerouslySetInnerHTML`; data is a static in-repo TS file |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan |
</threat_model>

## Artifacts this phase produces

- **Reskinned components:** `ReviewsSection` (#2d1a69 3-col masonry), `BuySection` (bundle wrapper, id `#bundle`), `ProductPurchaseBox` (restyled cards/summary/total-in-button). Same paths/default exports.
- **NOT modified:** `BuyNowButton.tsx` (checkout logic), `src/data/reviews.ts`.
- **No new i18n keys.**
- **Anchor id change:** `#buy` → `#bundle` (Hero/header "Order"/"Buy Now"/"Shop Now" target); new `#reviews` id on ReviewsSection; both gain `scroll-mt-[72px]`.
- **CSS utility usage:** `dips-purple-reviews`, `dips-card-review(-border)`, `dips-cream`, `dips-bundle-light(-border)`, `dips-bundle-summary`, `dips-card-ingredient-hl`, `border-brand-orange`, `text-heading-side`, `rounded-card-lg`, `p-card-padding`.
- **Assets consumed (existing):** `experience-couple-photo.png`, `product-box-small.png`.

<verification>
- `npm run build` completes with no TS/ESLint errors.
- `git diff --quiet src/components/BuyNowButton.tsx` passes (zero diff) and all 3 Stripe `priceId`s remain in `ProductPurchaseBox.tsx` — FUNC-01 preserved. (Final manual Stripe test-mode click-through is performed in Plan 05.)
- `src/data/reviews.ts` unchanged; ReviewsSection still imports it.
</verification>

<success_criteria>
- Reviews delivers the 05-UI-SPEC §6 look (flat #2d1a69, 3-col masonry, restyled cards) with all real reviews.
- Bundle selector delivers the §5 look (3 cards, 2x default + Most Popular, restyled summary, total-in-button) with pricing/checkout logic and BuyNowButton fully intact.
- Build is green; no message-file or data-source churn.
</success_criteria>

<output>
Create `.planning/phases/05-landing-page-sections/05-04-SUMMARY.md` when done.
</output>
