---
phase: 05-landing-page-sections
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - src/app/[locale]/page.tsx
  - src/app/[locale]/layout.tsx
  - src/components/WhyDipsSection.tsx
  - src/components/ProductSection.tsx
  - src/components/AboutSection.tsx
autonomous: false
requirements: [SECT-01, SECT-02, SECT-03, SECT-04, SECT-05, SECT-06, SECT-07, SECT-08, FUNC-01, FUNC-04]
must_haves:
  truths:
    - "Visiting the home renders all 8 redesigned sections in Figma order: trust bar/header, hero, story, ingredients, bundle, reviews, FAQs, footer"
    - "The home uses LandingHeader/LandingFooter (not the shared Header/Footer)"
    - "Anchor nav jumps land correctly under the sticky 72px header (scroll-smooth + scroll-mt)"
    - "The dead components WhyDipsSection/ProductSection/AboutSection are removed with no dangling imports"
    - "Clicking Buy Now in the redesigned bundle selector completes a real Stripe test-mode checkout"
    - "No Figma typos or placeholder support email remain in the built sections"
  artifacts:
    - path: "src/app/[locale]/page.tsx"
      provides: "Home wiring: 8 redesigned sections in order with LandingHeader/LandingFooter"
      contains: "LandingHeader"
    - path: "src/app/[locale]/layout.tsx"
      provides: "scroll-smooth on root <html> for anchor navigation"
      contains: "scroll-smooth"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "all 8 section components"
      via: "imports LandingHeader, Hero, StorySection, IngredientsSection, BuySection, ReviewsSection, FAQSection, LandingFooter"
      pattern: "StorySection"
    - from: "src/app/[locale]/layout.tsx"
      to: "anchor scroll behavior"
      via: "scroll-smooth appended to root <html> className"
      pattern: "scroll-smooth"
---

<objective>
Wire the 8 redesigned sections into the home page in Figma order, swap the shared `Header`/`Footer` for `LandingHeader`/`LandingFooter` (home only), enable smooth anchor scrolling, delete the three now-dead components, and run the phase's definition-of-done gate (build + FUNC-04 grep sweep + regression diff + manual walkthrough + Stripe click-through). This is the integration + cleanup + verification plan; it depends on Plans 01-04 delivering all section components first.

Purpose: closes the phase goal — the home renders all 8 redesigned sections with the bundle selector firing the real Stripe checkout — and confirms nothing out of scope regressed (05-CONTEXT.md verification approach).

Output: rewired `src/app/[locale]/page.tsx` + `layout.tsx`; deleted `WhyDipsSection.tsx`, `ProductSection.tsx`, `AboutSection.tsx`; verified build + walkthrough.
</objective>

<execution_context>
@C:/dev/dips/lp-dips/.claude/gsd-core/workflows/execute-plan.md
@C:/dev/dips/lp-dips/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-landing-page-sections/05-UI-SPEC.md
@.planning/phases/05-landing-page-sections/05-CONTEXT.md

# Files being rewired
@src/app/[locale]/page.tsx
@src/app/[locale]/layout.tsx

<interfaces>
Section components produced by Plans 01-04 (all default exports):
  LandingHeader (Plan 01), LandingFooter (Plan 01) — client
  Hero (Plan 02, id #hero), StorySection (Plan 02, id #story) — client
  IngredientsSection (Plan 03, id #ingredients), FAQSection (Plan 03, id #faq) — client
  ReviewsSection (Plan 04, id #reviews), BuySection (Plan 04, id #bundle) — BuySection is a SERVER component needing the `locale` prop
Current page.tsx passes `<BuySection locale={locale} />` — keep that prop.
layout.tsx root: `<html lang={locale} suppressHydrationWarning className={`${plusJakartaSans.variable} ${dmSans.variable}`}>` — append `scroll-smooth` to this className.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewire page.tsx (8 sections, Figma order, LandingHeader/Footer) + scroll-smooth on layout root</name>
  <files>src/app/[locale]/page.tsx, src/app/[locale]/layout.tsx</files>
  <read_first>
    - src/app/[locale]/page.tsx (current wiring + import list to replace)
    - src/app/[locale]/layout.tsx (root `<html className={...}>` to extend)
    - .planning/phases/05-landing-page-sections/05-UI-SPEC.md ("Section-by-Section Contract" order + "Out-of-Scope Page Contract")
  </read_first>
  <action>
    Rewrite the imports and JSX of `src/app/[locale]/page.tsx` so the home renders exactly these components in this order inside `<main>`: `LandingHeader`, `Hero`, `StorySection`, `IngredientsSection`, `BuySection` (with `locale={locale}`), `ReviewsSection`, `FAQSection`, `LandingFooter`. Import `LandingHeader` and `LandingFooter` from `@/components/LandingHeader` / `@/components/LandingFooter` and `StorySection` from `@/components/StorySection`. REMOVE the imports and JSX for `Header`, `Footer`, `AboutSection`, `ProductSection`, and `WhyDipsSection` (these are no longer used on the home; `Header`/`Footer` remain imported by the 9 other pages — do not touch those). Keep `HomePage` an async server component that awaits `params` for `locale`. In `src/app/[locale]/layout.tsx`, append `scroll-smooth` to the root `<html>` element's `className` template literal (so it reads `${plusJakartaSans.variable} ${dmSans.variable} scroll-smooth`) — this makes header anchor links glide, and combined with each section's `scroll-mt-[72px]` (added in Plans 01-04) lands anchor jumps below the sticky 72px header.
  </action>
  <verify>
    <automated>grep -q "LandingHeader" src/app/[locale]/page.tsx && grep -q "LandingFooter" src/app/[locale]/page.tsx && grep -q "StorySection" src/app/[locale]/page.tsx && grep -q "BuySection locale={locale}" src/app/[locale]/page.tsx && ! grep -q "WhyDipsSection" src/app/[locale]/page.tsx && ! grep -q "AboutSection" src/app/[locale]/page.tsx && ! grep -q "ProductSection" src/app/[locale]/page.tsx && grep -q "scroll-smooth" src/app/[locale]/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` imports and renders LandingHeader, Hero, StorySection, IngredientsSection, BuySection (locale prop), ReviewsSection, FAQSection, LandingFooter in that exact order.
    - `page.tsx` no longer references Header, Footer, AboutSection, ProductSection, or WhyDipsSection.
    - `layout.tsx` root `<html>` className includes `scroll-smooth` (alongside the two font variables).
    - `page.tsx` remains an async server component passing `locale` to BuySection.
  </acceptance_criteria>
  <done>Home renders all 8 redesigned sections in Figma order with LandingHeader/LandingFooter and smooth anchor scrolling; no dead imports remain.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete dead components + FUNC-04 grep sweep + build gate</name>
  <files>src/components/WhyDipsSection.tsx, src/components/ProductSection.tsx, src/components/AboutSection.tsx</files>
  <read_first>
    - src/app/[locale]/page.tsx (confirm — after Task 1 — that none of the three files are imported anywhere on the home path)
    - .planning/phases/05-landing-page-sections/05-CONTEXT.md ("Component Cleanup & File Organization" — delete once absorbed/replaced, no dead code)
    - .planning/phases/05-landing-page-sections/05-UI-SPEC.md ("Copywriting Contract" — FUNC-04 typo + email corrections)
  </read_first>
  <action>
    Delete the three now-orphaned components: `src/components/WhyDipsSection.tsx` (dropped entirely — no Figma counterpart), `src/components/ProductSection.tsx` (its 4 feature cards were absorbed into Hero in Plan 02), and `src/components/AboutSection.tsx` (its copy was folded into StorySection in Plan 02). Before deleting, confirm via grep that no remaining file imports them; if any non-home file still imports one, STOP and report rather than breaking that page. Then run the FUNC-04 correctness sweep across `src/` and `messages/`: confirm none of the Figma misspellings and none of the Figma placeholder support mailbox strings survive in the built sections, and confirm the real support email is present in the footer (the exact literals to search for are encoded in this task's verify/acceptance grep commands, not restated here to avoid seeding them into source). Finally run the build gate.
  </action>
  <verify>
    <automated>test ! -f src/components/WhyDipsSection.tsx && test ! -f src/components/ProductSection.tsx && test ! -f src/components/AboutSection.tsx && ! grep -rqi "Igredient" src/ messages/ && ! grep -rqi "Aphrodiasiac" src/ messages/ && ! grep -rqi "Gaurantee" src/ messages/ && ! grep -rq "tabs.co" src/ && ! grep -rq "help@dips.co" src/ && grep -q "info@dipschocolate.com" src/components/LandingFooter.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `WhyDipsSection.tsx`, `ProductSection.tsx`, `AboutSection.tsx` no longer exist on disk.
    - No remaining file imports any of the three deleted components (build succeeds, proving no dangling imports).
    - Repo-wide grep across `src/` and `messages/` finds zero occurrences of the three Figma typos and zero occurrences of the Figma placeholder support mailbox strings.
    - The real support email `info@dipschocolate.com` is present in `LandingFooter.tsx`.
    - `npm run build` (`prisma generate && next build`) exits 0 with no TypeScript or ESLint errors.
  </acceptance_criteria>
  <done>Dead components removed with no dangling imports; FUNC-04 sweep clean; full build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — full home walkthrough + Stripe checkout + out-of-scope regression</name>
  <action>Pause for human verification (do not auto-approve). Present the what-built summary and the numbered how-to-verify steps below, then wait for the resume signal before marking the phase complete.</action>
  <what-built>
    The complete redesigned home page: all 8 Figma sections (trust bar/header, hero, story, ingredients, bundle selector, reviews, FAQs, footer) wired into `src/app/[locale]/page.tsx` with `LandingHeader`/`LandingFooter`, smooth anchor scrolling, and the bundle selector still driving the real Stripe checkout. `npm run build` already passed and the FUNC-04 grep sweep is clean; this checkpoint is the manual browser + checkout confirmation that automation cannot fully assert (no test framework exists in this repo — 05-CONTEXT.md).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open `http://localhost:3000/en`.
    2. Confirm all 8 sections render in order and match 05-UI-SPEC.md desktop (1440px) intent: dark trust bar + minimal nav; gradient hero with two-tone H1 + 4 feature cards; split Story; split Ingredients (6 cards, cocoa highlighted); bundle selector with 2x pre-selected + "Most Popular"; #2d1a69 reviews in 3 columns; accordion FAQ (open item = orange left border + rotated chevron); dark footer with newsletter + Affiliates link.
    3. Click each header nav link (Menu / Our Story / Order / Contact) and Hero's "Buy Now" + "How It Works?" — confirm each scrolls smoothly to the correct section and lands BELOW the sticky header (not hidden under it).
    4. FUNC-01: with Stripe in test mode, click "Buy Now" in the bundle selector (2x default). Confirm it redirects to Stripe checkout with the 2-box line item / total consistent with `ProductPurchaseBox.tsx`'s real prices (NOT the Figma $59.99). Optionally switch to 1x and 3x and confirm the priceId/total updates.
    5. Regression: run `git status` and confirm `src/components/Header.tsx`, `src/components/Footer.tsx`, and `src/components/BuyNowButton.tsx` show NO changes — the 9 shared pages and the checkout call are untouched. Spot-check one shared page (e.g. `/en/product/dips-chocolate`) still renders the OLD Header/Footer normally.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 sections render correctly, anchors scroll under the header, the Stripe checkout fires with real prices, and the shared pages/checkout are unchanged — otherwise describe what is off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/stripe/create-checkout-session | Confirmed unchanged (BuyNowButton untouched across the phase) |
| home render tree → shared pages | Home now uses LandingHeader/LandingFooter; shared pages must stay on Header/Footer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | out-of-scope page regression | mitigate | `git status` must show zero diff on `Header.tsx`/`Footer.tsx`/`BuyNowButton.tsx`; deletions verified not to break any importer via full `next build` |
| T-05-13 | Repudiation/Integrity | checkout still fires real prices | mitigate | Manual Stripe test-mode click-through confirms the redirect + real line items (FUNC-01) |
| T-05-14 | Injection (XSS) | anchor `scroll-smooth`/nav | accept | Anchors are static in-app hashes; no user-controlled routing input |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan |
</threat_model>

## Artifacts this phase produces

- **Rewired route:** `src/app/[locale]/page.tsx` (8 redesigned sections in Figma order, LandingHeader/LandingFooter, home only).
- **Modified layout:** `src/app/[locale]/layout.tsx` (root `<html>` gains `scroll-smooth`).
- **Deleted components:** `WhyDipsSection.tsx`, `ProductSection.tsx`, `AboutSection.tsx`.
- **Verified invariants:** `Header.tsx`/`Footer.tsx`/`BuyNowButton.tsx` unchanged (FUNC-03/FUNC-01 regression protection); FUNC-04 typo/email sweep clean repo-wide.
- **No new i18n keys or assets.**

<verification>
- `npm run build` exits 0 (Task 2 gate).
- FUNC-04 grep sweep across `src/`+`messages/` returns zero typo/placeholder-email hits; real support email present in footer.
- `git status` shows no diff on `Header.tsx`, `Footer.tsx`, `BuyNowButton.tsx`.
- Human-verify checkpoint approved (8 sections render, anchors land under header, Stripe checkout fires with real prices, shared pages unchanged).
</verification>

<success_criteria>
- The home renders all 8 redesigned sections in Figma order using LandingHeader/LandingFooter with working smooth anchor navigation.
- The bundle selector completes a real Stripe test-mode checkout (FUNC-01).
- The three dead components are deleted with no dangling imports and a green build.
- No Figma typos or placeholder support email remain; shared pages + checkout unchanged (FUNC-03/FUNC-04).
</success_criteria>

<output>
Create `.planning/phases/05-landing-page-sections/05-05-SUMMARY.md` when done.
</output>
