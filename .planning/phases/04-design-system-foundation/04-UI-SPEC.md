---
phase: 4
slug: design-system-foundation
status: draft
shadcn_initialized: true
preset: "style: new-york, baseColor: neutral, cssVariables: true (existing components.json — not reconfigured this phase)"
created: 2026-07-16
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for the design-system foundation: Figma tokens in the Tailwind 4 theme, licensed font wiring, and CTA orange normalization. Consumed by Phase 5 (Landing Page Sections) and Phase 6 (Responsive/i18n). Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Source of truth for all raw values:** `scratchpad/figma/FIGMA-EXTRACTION.md` (must be copied into the repo during Phase 4 execution — see Assets section). Every hex/px value below is transcribed from that document; do not re-derive from screenshots.

**Out of scope for this contract:** section layouts, component composition, responsive breakpoints, i18n copy (Phases 5–6). This document defines only the shared tokens, fonts, and the normalized CTA orange that those phases consume. Focal point/visual hierarchy within a section is deferred to Phase 5 (section layouts) — this is a token/font/color foundation, not a layout spec.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `components.json` present, not reconfigured this phase) |
| Preset | style: new-york, baseColor: neutral, cssVariables: true, prefix: none |
| Component library | Radix UI primitives (existing), shadcn `new-york` style |
| Icon library | lucide-react (existing) |
| Font | 4 families, mixed strategy — 2 existing licensed OTFs kept as-is, 2 new Google Fonts added via `next/font/google` (see Typography) |
| CSS entry | `src/app/globals.css` (Tailwind 4, `@theme` block — NOT `tailwind.config.ts`) |
| Existing tokens preserved | `--color-brand-*` (8 tokens, 283 usages across 41 files in pages outside this milestone's scope) — do not rename, remove, or repurpose |

**Investigation finding (locks the approach below):** `globals.css` already declares `@font-face` for `AllRoundGothic` (700, `public/fonts/all-round-gothic-bold.otf`) and `FilsonPro` (400/500/700, `public/fonts/FilsonPro{Regular,Medium,Bold}.otf`), exposed as `--font-heading`/`--font-body` and actively used sitewide (Hero, AboutSection, BuySection, ProductSection, ReviewsSection, WhyDipsSection + product/order/profile pages). These are real production-licensed assets, not the Figma file's flagged "FONTSPRING DEMO" fonts. Confirmed decision: reuse them rather than substituting with Google Fonts (see Typography).

**Confirmed decision:** `--color-brand-orange: #f27521` already exists in the theme and matches the brand book. Confirmed decision: this is the single normalized CTA orange (see Color).

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

**Exceptions (Figma-locked, do not round to the 8pt scale):**

| Token | Value | Usage | Why an exception |
|-------|-------|-------|-------------------|
| `--spacing-card-padding` | 25px | Recurring card padding: ingredient cards, review cards, bundle cards, FAQ accordion items | Figma consistently uses 25px (not 24px) across all card families in the extraction — a real, repeated design decision, not a rounding artifact. Add as a named token in `@theme` rather than an arbitrary `p-[25px]` scattered across Phase 5 components. |

One-off layout constants from the Figma extraction (trust bar height 45px, header height 72px, header top offset 67px, header horizontal padding 40px, hero section height 1054px, etc.) are **not** tokenized here — they occur once each and belong to Phase 5 section implementation, not the shared foundation. Phase 5 may use arbitrary Tailwind values (e.g. `h-[72px]`) for these directly, citing `FIGMA-EXTRACTION.md`.

---

## Typography

Canonical 4-role summary (per template convention):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 18px | 400 (FilsonPro Regular) | 1.6 |
| Label | 14px | 500 (FilsonPro Medium) | 1.2 |
| Heading | 54px | 700 (AllRoundGothic Bold) | 1.15 |
| Display | 64px | 700 (AllRoundGothic Bold) | 1.05 |

**Typography Exception (declared, not a gap):** the canonical table above is the summary view only — it is not the real constraint for this phase. The "Extended type scale" below declares 4 font families, 16 named sizes, and 4 distinct weights (400/500/700 plus 400-italic), exceeding the standard max-4-size / max-2-weight caps. This is an intentional, bounded exception, not scope creep:

1. **Sizes/weights are locked 1:1 to the client's Figma extraction**, verified against `FIGMA-EXTRACTION.md` by the checker — every row in the Extended type scale table traces to a specific Figma text layer, not an invented value. A marketing landing page with 9 distinct sections has a legitimately richer type hierarchy than a typical app screen.
2. **Two of the four families are pre-existing production fonts already shipped sitewide** (`AllRoundGothic` 700, `FilsonPro` 400/500/700) — the extended scale mirrors typography that already exists in `globals.css` and is already rendered on 41 files across the app. It does not introduce new font-loading debt; it only adds `Plus Jakarta Sans` and `DM Sans` for the two roles with no existing asset.
3. The exception is **named and bounded**: every size/weight/family combination is enumerated in the Extended type scale table below, with a single Figma usage citation each — Phase 5 must draw from this closed list, not invent additional sizes/weights ad hoc.

Future readers (Phase 5 executor, Phase 5/6 checkers): treat the canonical 4-role table above as a quick-reference summary only. The Extended type scale is the real, enforceable contract for this phase.

### Font family assignments (confirmed)

| Role | Family | Status | Theme token | Weight(s) used |
|------|--------|--------|-------------|-----------------|
| Headings (H1/H2, footer headings) | **AllRoundGothic** | Existing OTF, kept as-is — no substitution | `--font-heading` | 700 only (no other weight file exists) |
| Body / paragraphs / footer links | **FilsonPro** | Existing OTF, kept as-is — no substitution | `--font-body` | 400 regular, 500 medium (nav/trust-bar/labels), 700 bold (rare emphasis) |
| Subtitle italic | **FilsonPro** (synthetic italic) | `font-style: italic` applied to the existing Regular face — no dedicated italic OTF exists | `--font-body` + `font-style: italic` in the component, e.g. `italic font-body` | 400. If DIPS supplies a real Filson Pro Italic OTF later, swap in a new `@font-face` block at 400/italic — no token rename needed. |
| Card titles/body (ingredient cards, bundle cards, review cards) | **Plus Jakarta Sans** (Satoshi substitute) | NEW — wire via `next/font/google` | `--font-card` | 700 (card titles), 400 (card body) |
| CTA buttons | **DM Sans** | NEW — wire via `next/font/google` (already a Google Font per REQUIREMENTS.md) | `--font-cta` | 700 |
| Nav links / trust bar | **FilsonPro** (absorbed — no 5th family) | Reuses existing Medium weight instead of introducing Inter | `--font-body` | 500 |

Rationale for Plus Jakarta Sans over Manrope: closer match to Satoshi's rounded/geometric character per the phase's "rounded geometric" brief.

Implementation note for `--font-card` / `--font-cta`: use `next/font/google` in a shared layout/font module (e.g. `src/lib/fonts.ts`), export the generated CSS variable names, and register them in the `@theme` block as `--font-card: var(--font-plus-jakarta-sans), sans-serif;` / `--font-cta: var(--font-dm-sans), sans-serif;` — do not `@import` Google Fonts via CSS (breaks Next.js font optimization).

### Extended type scale (Figma-locked sizes → named tokens)

| Token | Size | Line height | Weight | Family | Figma usage |
|-------|------|-------------|--------|--------|-------------|
| `display-hero` | 64px | 1.05 | 700 | AllRoundGothic | H1 hero ("The Chocolate that changes the night.") |
| `heading-lg` | 54px | 1.15 | 700 | AllRoundGothic | H2 — Reviews, FAQs, Story |
| `heading-md` | 48px | 1.15 | 700 | AllRoundGothic | H2 — "Behind the experience" (Ingredients intro) |
| `heading-side` | 64px | 1.05 | 700 | AllRoundGothic | Story "Made to be Savoured by Two", Bundle "Bring the Experience home." |
| `subtitle-italic-lg` | 24px | 1.4 | 400 italic | FilsonPro | Hero/section subtitles |
| `subtitle-italic-sm` | 20px | 1.4 | 400 italic | FilsonPro | "The ingredients" |
| `body-lg` | 24px | 1.5 | 400 | FilsonPro | Story paragraphs |
| `body-md` | 18px | 1.6 | 400 | FilsonPro | Review text, FAQ answers, footer links |
| `card-title-lg` | 24px | 1.2 | 700 | Plus Jakarta Sans | Bundle card name, cocoa expanded card title |
| `card-title-sm` | 18px | 1.2 | 700 | Plus Jakarta Sans | Ingredient card name |
| `card-body` | 14px | 1.5 | 400 | Plus Jakarta Sans | Card descriptions (14–18px range in Figma; 14px is the base, 18px cases use `body-md`) |
| `cta-button` | 16px | 1.1 | 700 | DM Sans | All CTA buttons/pills |
| `nav-link` | 14px | 1.2 | 500 | FilsonPro | Header nav |
| `trust-bar` | 12px | 1.2 | 500 | FilsonPro | Trust bar items |
| `footer-heading` | 22px | 1.2 | 700 | AllRoundGothic | Footer column headings |
| `footer-fine` | 14px | 1.5 | 400 | FilsonPro | Copyright / FDA disclaimer |

Letter-spacing is not specified in the Figma extraction. The existing Hero.tsx already applies tight tracking on large headings (`tracking-[-0.04em]` to `tracking-[-0.05em]`) — Phase 5 should follow this existing convention for `display-hero`/`heading-*` roles for visual consistency with the rest of the site. Not a blocking requirement of this phase.

---

## Color

Two-tier system: existing `--color-brand-*` tokens (untouched, power all out-of-scope pages) and new `--color-dips-*` tokens (redesign-only, dark-purple landing page). Namespace confirmed by coordinator — no collisions.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--color-dips-purple-section: #1a0a2e` | Dark purple surface family across the redesigned landing page (Ingredients, FAQs; sibling values below cover Hero/Footer/Reviews variants of the same role) |
| Secondary (30%) | `--color-dips-card-ingredient: #231435` | Cards, panels; also `--color-dips-bundle-light: #ffffff` and `--color-dips-cream: #fff8f0` for the Bundle section's light-panel variant |
| Accent (10%) | `--color-brand-orange: #f27521` (existing token, reused) | CTA buttons ("Buy Now"/"Shop Now"/"Sign Up"), bundle-selected card border, "Most Popular" badge, decorative diamond glyphs, active carousel arrow, footer mailto link, FAQ open-item border + chevron, ingredient badge-pill accents (RITUAL/STAMINA/SPARK/PRESENCE/UNION/BALANCE) |
| Destructive | N/A | No destructive actions in this landing-page redesign phase |

Accent reserved for: CTA buttons/pills only, bundle-selected card border (2px), "Most Popular" floating badge, decorative diamond squares (rotate 43°), active carousel arrow fill, FAQ open-item left border + chevron, footer "Sign Up" button + mailto link color, ingredient badge-pill text/border. Never used as a general text or surface color.

### CTA orange normalization (DSGN-03 — confirmed)

The Figma file uses three near-identical, inconsistent oranges (designer oversight, not an intentional palette). All map to the single existing token `--color-brand-orange: #f27521`:

| Figma hex | Where it appeared | Normalizes to |
|-----------|--------------------|----------------|
| `#fb6c04` | Hero "Shop Now"/"Buy Now" CTAs | `--color-brand-orange` |
| `#ff6b01` | Bundle "Buy Now" CTA, bundle-selected card border, "Most Popular" badge | `--color-brand-orange` |
| `#f15a22` | Footer logo, footer "Sign Up" button, mailto link color | `--color-brand-orange` |
| `#f16b16` | Decorative diamond glyphs, active carousel arrow (not itself a CTA, but same accidental-drift family) | `--color-brand-orange` (recommendation — one true orange across the page; flag to designer only if visually jarring during Phase 5 review) |

Phase 5 components must reference `brand-orange` (via `bg-brand-orange`, `border-brand-orange`, `text-brand-orange`) — never hardcode any of the four hex values above.

### New `--color-dips-*` tokens (add to `@theme` in `globals.css`, alongside existing `--color-brand-*`)

| Token | Value | Figma role / usage |
|-------|-------|---------------------|
| `--color-dips-purple-hero-start` | `#18012d` | Hero background gradient, stop 0% |
| `--color-dips-purple-hero-mid` | `#200635` | Hero background gradient, stop 57.4% |
| `--color-dips-purple-hero-end` | `#250246` | Hero background gradient, stop 100% |
| `--color-dips-purple-deepest` | `#0a0519` | Story right panel ("Our Story"), Footer background |
| `--color-dips-purple-section` | `#1a0a2e` | Ingredients section bg, FAQs section bg |
| `--color-dips-purple-reviews` | `#2d1a69` | Reviews section background |
| `--color-dips-cream` | `#fff8f0` | Bundle selector light panel (distinct from existing `--color-brand-cream: #f3e9e3`) |
| `--color-dips-card-tint` | `rgba(49,34,89,0.25)` | Hero feature cards fill |
| `--color-dips-card-tint-border` | `#392a61` | Hero feature cards border |
| `--color-dips-card-tint-2` | `rgba(38,23,77,0.15)` | "10,000+ Happy Couples" social-proof badge fill |
| `--color-dips-card-tint-2-border` | `rgba(82,64,130,0.5)` | Social-proof badge border |
| `--color-dips-card-lavender` | `rgba(166,145,221,0.15)` | Mini-badge pills across sections, fill |
| `--color-dips-card-lavender-border` | `rgba(212,201,244,0.25)` | Mini-badge pills border |
| `--color-dips-card-ingredient` | `#231435` | Ingredient cards fill, FAQ collapsed item fill |
| `--color-dips-card-ingredient-border` | `#39294c` | Ingredient cards border, FAQ collapsed item border |
| `--color-dips-card-ingredient-hl` | `#371629` | Highlighted cocoa ingredient card fill; RITUAL/STAMINA/etc. badge fill; bundle-selected card fill (same value reused across all three — confirmed single token, not a coincidence) |
| `--color-dips-card-ingredient-hl-border` | `#5b2f2d` | Highlighted cocoa card border |
| `--color-dips-card-review` | `rgba(35,20,53,0.4)` | Review card fill |
| `--color-dips-card-review-border` | `rgba(57,41,76,0.4)` | Review card border |
| `--color-dips-bundle-light` | `#ffffff` | Unselected bundle cards (1x/3x) fill |
| `--color-dips-bundle-light-border` | `#eee6df` | Unselected bundle cards border; bundle summary row border |
| `--color-dips-bundle-summary` | `#f6efe9` | Bundle summary row (Unit Price/Shipping) fill |
| `--color-dips-text-lavender` | `#ebd9fe` | Subtitles, body text on dark backgrounds; FAQ answer text |
| `--color-dips-text-lavender-muted` | `#ae9bda` | Nav links, trust bar text, ingredients intro copy |
| `--color-dips-text-mauve` | `#96838f` | Card description text, price/box text |
| `--color-dips-text-gray-review` | `#9499a9` | Review body text |
| `--color-dips-text-footer` | `#706c7b` | Footer body text (tier 1) |
| `--color-dips-text-footer-secondary` | `#656170` | Footer body text (tier 2) |
| `--color-dips-text-footer-tertiary` | `#74707f` | Footer body text (tier 3) |
| `--color-dips-text-footer-fine` | `#464253` | Copyright / FDA disclaimer text |
| `--color-dips-text-purple-deep` | `#39165e` | Text on light cards (bundle unselected cards) |
| `--color-dips-text-headline-lilac` | `#cfa9f6` | The word "Chocolate" in the hero H1 |

Bundle-selected card border reuses `--color-brand-orange` (2px), per the CTA orange normalization above — it is not a new dips-* token.

---

## Radius

| Token | Value | Usage |
|-------|-------|-------|
| `--radius-card` (new) | 15px (`0.9375rem`) | Ingredient cards, bundle cards, FAQ accordion items, hero feature cards |
| `--radius-card-lg` (new) | 18px (`1.125rem`) | Review cards |
| `rounded-full` (existing Tailwind utility, no new token needed) | 9999px | CTA buttons/pills (height 50px), newsletter input (height 46px), avatar circles, carousel arrow circles (60px) |

Added alongside the existing `--radius` / `--radius-xl` / `--radius-2xl` / `--radius-3xl` scale — none of the existing radius tokens are modified or removed.

Not tokenized (one-off, Phase 5 implementation detail): decorative diamond squares (5–8px, `rotate-43°`, `bg-brand-orange` per normalization above).

---

## Copywriting Contract

This phase introduces no new UI copy (foundation-only — no components render yet). It locks the **correction rules** that Phase 5 must apply when transcribing Figma copy, and the naming convention Phase 5 must follow for the primary CTA.

| Element | Copy |
|---------|------|
| Primary CTA | Verb + noun per Figma, unchanged: "Buy Now" (hero, bundle), "Shop Now" (header). No new CTA introduced this phase. |
| Typo correction — Guarantee | Figma has "Gaurantee"/"Gauranteed" → must render as "Guarantee"/"Guaranteed" |
| Typo correction — Ingredients | Figma has "100% Natural Igredients" → must render as "100% Natural Ingredients" |
| Typo correction — Aphrodisiac | Figma has "Aphrodiasiac Herbal Blend" → must render as "Aphrodisiac Herbal Blend" |
| Contact email | Figma shows "help@dips.co" with a broken `mailto:help@tabs.co`. Must use the real, already-in-production address: `info@dipschocolate.com` (matches existing `src/components/Footer.tsx` convention — `orders@dipschocolate.com` is reserved for order-specific transactional email, not general contact) |
| Empty state | N/A — no data-driven empty states in this phase |
| Error state | N/A — no forms/interactive states in this phase (Phase 5's newsletter signup form, if it needs error copy, defines its own then) |
| Destructive confirmation | N/A — no destructive actions in a landing-page redesign |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | None — this phase adds no new shadcn components (tokens/fonts only) | not required |
| Third-party | none | not applicable |

`next/font/google` additions (Plus Jakarta Sans, DM Sans) are first-party Next.js API usage, not a shadcn registry concern.

---

## Assets

The 27 Figma-extracted assets currently live only in the session scratchpad (`scratchpad/figma/assets/`) and must be copied into the repo during Phase 4 execution, before Phase 5 can reference them.

**Destination (confirmed):** `public/images/redesign/` — flat structure, matching the existing flat `public/images/` convention. File names preserved as extracted (e.g. `hero-product.png`, `blob-vector-1.svg`, `ing-icon-cocoa.png`, `footer-logo-orange.svg`).

**Font files:** No new font files to copy — `AllRoundGothic`/`FilsonPro` OTFs already live in `public/fonts/` (unchanged); Plus Jakarta Sans/DM Sans load via `next/font/google` (no local files).

---

## Out-of-Scope Page Contract

Reminder locked for execution and for the Phase 4/5/6 checker: pages outside this milestone (admin dashboard, affiliate dashboard/join/login, order confirmation, product pages, legal pages) must remain visually and functionally unchanged. They depend on the existing `--color-brand-*` tokens (283 usages / 41 files) and the existing `AllRoundGothic`/`FilsonPro` `@font-face` declarations, which this phase does not modify or remove — it only adds new `--color-dips-*` tokens, `--radius-card`/`--radius-card-lg`, `--font-card`/`--font-cta`, and a spacing exception, all additive to `globals.css`.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS (declared exception approved — see Typography Exception callout)
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-07-16 (1 revision round — typography exception + visuals scope note)
