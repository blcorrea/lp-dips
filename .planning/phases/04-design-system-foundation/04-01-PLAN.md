---
phase: 04-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-03]
must_haves:
  truths:
    - "The @theme block exposes every --color-dips-* Figma color from the UI-SPEC Color table as a named token (32 tokens)"
    - "--color-brand-orange (#f27521) remains the single normalized CTA orange; none of the Figma drift oranges (#fb6c04/#ff6b01/#f15a22/#f16b16) are added as tokens (DSGN-03)"
    - "--radius-card (0.9375rem / 15px) and --radius-card-lg (1.125rem / 18px) exist alongside the existing radius scale"
    - "A named spacing scale (xs–3xl) plus --spacing-card-padding (25px) exists"
    - "All 16 --text-* typography role tokens from the Extended type scale exist, each with a matching --text-*--line-height"
    - "--font-card and --font-cta are registered in @theme, pointing at var(--font-plus-jakarta-sans) and var(--font-dm-sans)"
    - "Existing --color-brand-* tokens, --font-heading/--font-body, and the AllRoundGothic/FilsonPro @font-face blocks are unchanged"
  artifacts:
    - path: "src/app/globals.css"
      provides: "All new @theme design tokens for the landing redesign"
      contains: "--color-dips-purple-hero-start"
  key_links:
    - from: "src/app/globals.css (--font-card)"
      to: "src/lib/fonts.ts (variable --font-plus-jakarta-sans)"
      via: "--font-card: var(--font-plus-jakarta-sans), sans-serif"
      pattern: "--font-card:\\s*var\\(--font-plus-jakarta-sans\\)"
    - from: "src/app/globals.css (--font-cta)"
      to: "src/lib/fonts.ts (variable --font-dm-sans)"
      via: "--font-cta: var(--font-dm-sans), sans-serif"
      pattern: "--font-cta:\\s*var\\(--font-dm-sans\\)"
---

<objective>
Add every Figma design token to the Tailwind 4 `@theme` block in `src/app/globals.css`: the 32 redesign-only `--color-dips-*` colors, the two new radius tokens, the named spacing scale plus the 25px card-padding exception, the 16 `--text-*` typography roles, and the `--font-card`/`--font-cta` registrations. Confirm `--color-brand-orange` as the single normalized CTA orange (DSGN-03). All changes are strictly additive — no existing token, font-face, or brand color is renamed, removed, or repurposed.

Purpose: Phase 5 sections reference these named tokens instead of scattering raw Figma hex/px values across components. This is the color/radius/spacing/typography half of the shared foundation (DSGN-01, DSGN-03, and the font-token registration half of DSGN-02).
Output: An expanded `@theme` block in `src/app/globals.css` containing all named foundation tokens.
</objective>

<execution_context>
@C:/dev/dips/lp-dips/.claude/gsd-core/workflows/execute-plan.md
@C:/dev/dips/lp-dips/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-system-foundation/04-UI-SPEC.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 32 --color-dips-* tokens and confirm CTA orange normalization</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (the current @theme block, lines 46-76 — existing --color-brand-* tokens including --color-brand-orange: #f27521)
    - .planning/phases/04-design-system-foundation/04-UI-SPEC.md (Color section: "New --color-dips-* tokens" table and "CTA orange normalization" table)
  </read_first>
  <action>
    Inside the existing `@theme` block in src/app/globals.css, after the existing `--color-brand-*` group, add a new commented section for the redesign colors and transcribe all 32 rows of the UI-SPEC "New --color-dips-* tokens" table verbatim, one CSS custom property per row (e.g. --color-dips-purple-hero-start: #18012d; --color-dips-purple-section: #1a0a2e; --color-dips-card-ingredient: #231435; --color-dips-text-headline-lilac: #cfa9f6; including the rgba() valued tokens such as --color-dips-card-tint: rgba(49,34,89,0.25)). Copy hex/rgba values exactly from the UI-SPEC — do not re-derive from screenshots or round values. Note that --color-dips-cream (#fff8f0) is intentionally distinct from the existing --color-brand-cream (#f3e9e3): both must coexist.
    Do NOT modify, rename, or remove any existing --color-brand-* token. Do NOT add the Figma drift oranges (#fb6c04, #ff6b01, #f15a22, #f16b16) as tokens — per DSGN-03 they all normalize to the already-existing --color-brand-orange: #f27521, which stays as the single CTA orange. Leave the existing --color-brand-orange line untouched.
    Use a section comment such as "DIPS REDESIGN COLORS (landing-only, additive)" but do NOT put the substring used by the verify greps inside comment text.
  </action>
  <verify>
    <automated>test $(grep -c -- '--color-dips-' src/app/globals.css) -eq 32 && grep -q -- '--color-brand-orange: #f27521' src/app/globals.css && echo TOKENS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c -- '--color-dips-' src/app/globals.css` returns exactly 32.
    - Source assertion: --color-dips-cream and --color-brand-cream both present with their distinct values (#fff8f0 and #f3e9e3).
    - Source assertion: the existing `--color-brand-orange: #f27521` line is still present and unchanged.
    - Behavior assertion: no line matching any of the Figma drift oranges (#fb6c04, #ff6b01, #f15a22, #f16b16) exists in globals.css.
    - Source assertion: existing --color-brand-purple / --color-brand-cream / --color-brand-charcoal etc. are byte-for-byte unchanged.
  </acceptance_criteria>
  <done>All 32 --color-dips-* tokens present with exact UI-SPEC values; --color-brand-orange unchanged and confirmed as the sole CTA orange; no Figma drift orange added; existing brand tokens untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add radius tokens and the named spacing scale</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (existing radius scale lines 53-56: --radius / --radius-xl / --radius-2xl / --radius-3xl)
    - .planning/phases/04-design-system-foundation/04-UI-SPEC.md (Radius section and Spacing Scale section, including the 25px card-padding exception)
  </read_first>
  <action>
    In the same `@theme` block, add the two new radius tokens from the UI-SPEC Radius table: --radius-card: 0.9375rem; (15px — ingredient/bundle/FAQ/hero feature cards) and --radius-card-lg: 1.125rem; (18px — review cards). Add them alongside the existing --radius/--radius-xl/--radius-2xl/--radius-3xl scale without modifying any existing radius token.
    Add the named spacing scale from the UI-SPEC Spacing Scale table as --spacing-* tokens: --spacing-xs: 4px; --spacing-sm: 8px; --spacing-md: 16px; --spacing-lg: 24px; --spacing-xl: 32px; --spacing-2xl: 48px; --spacing-3xl: 64px. Then add the Figma-locked exception --spacing-card-padding: 25px; (recurring 25px card padding — a real repeated design decision, not a rounding artifact). Do NOT tokenize the one-off layout constants (trust bar 45px, header 72px, hero 1054px, etc.) — those belong to Phase 5.
    These additions are purely additive; the existing Tailwind default numeric spacing scale remains available.
  </action>
  <verify>
    <automated>grep -q -- '--radius-card:' src/app/globals.css && grep -q -- '--radius-card-lg:' src/app/globals.css && grep -q -- '--spacing-card-padding: 25px' src/app/globals.css && grep -q -- '--spacing-3xl:' src/app/globals.css && echo RADIUS_SPACING_OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: --radius-card (0.9375rem) and --radius-card-lg (1.125rem) both present.
    - Source assertion: --spacing-xs through --spacing-3xl (7 named steps) all present with the UI-SPEC values.
    - Source assertion: --spacing-card-padding: 25px present.
    - Source assertion: existing --radius / --radius-xl / --radius-2xl / --radius-3xl lines are unchanged.
  </acceptance_criteria>
  <done>Two new radius tokens, the 7-step named spacing scale, and the 25px card-padding exception exist in @theme; existing radius/spacing behavior is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add the 16 --text-* typography roles and register --font-card / --font-cta</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (existing font tokens lines 48-51: --font-heading / --font-body / --font-sans)
    - .planning/phases/04-design-system-foundation/04-UI-SPEC.md (Typography section: "Extended type scale" table and the "Implementation note for --font-card / --font-cta")
  </read_first>
  <action>
    In the same `@theme` block, add the 16 typography roles from the UI-SPEC "Extended type scale" table as Tailwind 4 text tokens. For each row create a size token plus its paired line-height token using the Tailwind 4 idiom: e.g. --text-display-hero: 64px; --text-display-hero--line-height: 1.05; and likewise for heading-lg (54px/1.15), heading-md (48px/1.15), heading-side (64px/1.05), subtitle-italic-lg (24px/1.4), subtitle-italic-sm (20px/1.4), body-lg (24px/1.5), body-md (18px/1.6), card-title-lg (24px/1.2), card-title-sm (18px/1.2), card-body (14px/1.5), cta-button (16px/1.1), nav-link (14px/1.2), trust-bar (12px/1.2), footer-heading (22px/1.2), footer-fine (14px/1.5). Transcribe sizes and line-heights exactly from the UI-SPEC — weight and family are applied by Phase 5 via font utility classes, so do not encode weight into these tokens.
    Register the two new font-family tokens for the Google fonts wired in plan 04-02: --font-card: var(--font-plus-jakarta-sans), sans-serif; and --font-cta: var(--font-dm-sans), sans-serif;. The CSS variable names --font-plus-jakarta-sans and --font-dm-sans are the exact names that src/lib/fonts.ts (plan 04-02) exposes via next/font/google — they must match character-for-character. Do NOT @import Google Fonts via CSS (breaks Next.js font optimization); only reference the variables. Leave existing --font-heading / --font-body / --font-sans untouched.
  </action>
  <verify>
    <automated>test $(grep -c -- '--text-.*--line-height' src/app/globals.css) -ge 16 && grep -q -- '--font-card: var(--font-plus-jakarta-sans)' src/app/globals.css && grep -q -- '--font-cta: var(--font-dm-sans)' src/app/globals.css && echo TYPOGRAPHY_OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: at least 16 `--text-*--line-height` declarations exist (one per role).
    - Source assertion: --text-display-hero (64px) and --text-footer-fine (14px) present as spot checks against the UI-SPEC table.
    - Source assertion: --font-card resolves to var(--font-plus-jakarta-sans) and --font-cta to var(--font-dm-sans), matching the variable names plan 04-02 exports.
    - Source assertion: existing --font-heading / --font-body / --font-sans unchanged; no CSS @import for Google Fonts added.
  </acceptance_criteria>
  <done>All 16 typography role tokens (size + line-height) exist; --font-card and --font-cta are registered against the exact next/font variable names; existing font tokens preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan modifies only `src/app/globals.css` (CSS `@theme` custom properties). No runtime logic, no user input, no data handling, no auth surface, no new endpoints, no third-party package installs, no schema changes. There is no trust boundary crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | N/A | CSS theme tokens | accept | No attack surface — ships static CSS custom properties only. Out-of-scope pages preserved by additive-only edits (existing --color-brand-* / @font-face untouched). |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 04-01)

New `@theme` tokens added to `src/app/globals.css` (Phase 5 will reference these symbols — they are not drift):

**Colors (32):** `--color-dips-purple-hero-start`, `--color-dips-purple-hero-mid`, `--color-dips-purple-hero-end`, `--color-dips-purple-deepest`, `--color-dips-purple-section`, `--color-dips-purple-reviews`, `--color-dips-cream`, `--color-dips-card-tint`, `--color-dips-card-tint-border`, `--color-dips-card-tint-2`, `--color-dips-card-tint-2-border`, `--color-dips-card-lavender`, `--color-dips-card-lavender-border`, `--color-dips-card-ingredient`, `--color-dips-card-ingredient-border`, `--color-dips-card-ingredient-hl`, `--color-dips-card-ingredient-hl-border`, `--color-dips-card-review`, `--color-dips-card-review-border`, `--color-dips-bundle-light`, `--color-dips-bundle-light-border`, `--color-dips-bundle-summary`, `--color-dips-text-lavender`, `--color-dips-text-lavender-muted`, `--color-dips-text-mauve`, `--color-dips-text-gray-review`, `--color-dips-text-footer`, `--color-dips-text-footer-secondary`, `--color-dips-text-footer-tertiary`, `--color-dips-text-footer-fine`, `--color-dips-text-purple-deep`, `--color-dips-text-headline-lilac`.

**Reused (not new):** `--color-brand-orange` (#f27521) — the single normalized CTA orange (DSGN-03).

**Radius (2):** `--radius-card` (0.9375rem), `--radius-card-lg` (1.125rem).

**Spacing (8):** `--spacing-xs`, `--spacing-sm`, `--spacing-md`, `--spacing-lg`, `--spacing-xl`, `--spacing-2xl`, `--spacing-3xl`, `--spacing-card-padding` (25px).

**Typography (16 roles, each size + line-height):** `--text-display-hero`, `--text-heading-lg`, `--text-heading-md`, `--text-heading-side`, `--text-subtitle-italic-lg`, `--text-subtitle-italic-sm`, `--text-body-lg`, `--text-body-md`, `--text-card-title-lg`, `--text-card-title-sm`, `--text-card-body`, `--text-cta-button`, `--text-nav-link`, `--text-trust-bar`, `--text-footer-heading`, `--text-footer-fine`.

**Font family tokens (2):** `--font-card` → `var(--font-plus-jakarta-sans)`, `--font-cta` → `var(--font-dm-sans)` (the variables are provided by plan 04-02's `src/lib/fonts.ts`).
</artifacts_produced>

<verification>
- `grep -c -- '--color-dips-' src/app/globals.css` returns 32.
- `--radius-card`, `--radius-card-lg`, `--spacing-card-padding: 25px`, and the xs–3xl spacing scale are all present.
- At least 16 `--text-*--line-height` declarations exist; `--font-card` and `--font-cta` reference the correct next/font variable names.
- No Figma drift orange (#fb6c04/#ff6b01/#f15a22/#f16b16) appears anywhere in globals.css; existing `--color-brand-*` group and both @font-face families are unchanged.
- The @theme block still parses (no Tailwind build error introduced) — confirmed at the phase level by plan 04-02's `next build`.
</verification>

<success_criteria>
The Tailwind `@theme` exposes every Figma color, radius, spacing, and typography value as a named token, with `--color-brand-orange` as the single CTA orange — satisfying DSGN-01, DSGN-03, and the font-token registration portion of DSGN-02. Out-of-scope pages are untouched because every edit is additive.
</success_criteria>

<output>
Create `.planning/phases/04-design-system-foundation/04-01-SUMMARY.md` when done.
</output>
