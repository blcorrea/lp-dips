---
phase: 04-design-system-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/fonts.ts
  - src/app/[locale]/layout.tsx
autonomous: true
requirements: [DSGN-02]
must_haves:
  truths:
    - "Plus Jakarta Sans loads via next/font/google (weights 400 + 700) exposing the CSS variable --font-plus-jakarta-sans"
    - "DM Sans loads via next/font/google (weight 700) exposing the CSS variable --font-dm-sans"
    - "The storefront root layout applies both font variable classNames to <html>, making --font-plus-jakarta-sans and --font-dm-sans resolvable on every landing-page node"
    - "next build compiles the storefront without font-related errors"
    - "The existing AllRoundGothic/FilsonPro fonts and admin/ingredients layouts are unchanged"
  artifacts:
    - path: "src/lib/fonts.ts"
      provides: "next/font/google instances for Plus Jakarta Sans and DM Sans with exported CSS-variable class helpers"
      exports: ["plusJakartaSans", "dmSans"]
    - path: "src/app/[locale]/layout.tsx"
      provides: "font CSS variables applied on the storefront <html> element"
      contains: "variable"
  key_links:
    - from: "src/app/[locale]/layout.tsx (<html> className)"
      to: "src/lib/fonts.ts (plusJakartaSans.variable, dmSans.variable)"
      via: "className={`${plusJakartaSans.variable} ${dmSans.variable}`}"
      pattern: "plusJakartaSans\\.variable"
    - from: "src/lib/fonts.ts (variable --font-plus-jakarta-sans)"
      to: "src/app/globals.css (--font-card)"
      via: "next/font variable name consumed by @theme --font-card"
      pattern: "--font-plus-jakarta-sans"
---

<objective>
Wire the two new Google fonts for the redesign. Create `src/lib/fonts.ts` that loads Plus Jakarta Sans and DM Sans through `next/font/google` (the Next.js-optimized, self-hosted path — not a CSS `@import`), and apply their generated CSS-variable classNames to the storefront root layout `src/app/[locale]/layout.tsx` so `var(--font-plus-jakarta-sans)` and `var(--font-dm-sans)` resolve on every landing-page node. These are the variables that plan 04-01's `--font-card` / `--font-cta` tokens reference.

Purpose: Satisfy the font-loading half of DSGN-02. The two existing production OTFs (AllRoundGothic, FilsonPro) are kept as-is (per the UI-SPEC Typography Exception); this plan only adds the two families with no existing asset (Plus Jakarta Sans as the Satoshi substitute, DM Sans for CTAs).
Output: `src/lib/fonts.ts` (new) and a font-variable className on the storefront `<html>`.
</objective>

<execution_context>
@C:/dev/dips/lp-dips/.claude/gsd-core/workflows/execute-plan.md
@C:/dev/dips/lp-dips/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-system-foundation/04-UI-SPEC.md
@src/app/[locale]/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/fonts.ts with next/font/google loaders</name>
  <files>src/lib/fonts.ts</files>
  <read_first>
    - .planning/phases/04-design-system-foundation/04-UI-SPEC.md (Typography section: font family assignments table + the "Implementation note for --font-card / --font-cta")
    - src/app/[locale]/layout.tsx (to confirm this is a server component that can import next/font)
    - CLAUDE.md (import convention: absolute @/ imports; camelCase lib file naming)
  </read_first>
  <action>
    Create a new file src/lib/fonts.ts. Import Plus_Jakarta_Sans and DM_Sans from 'next/font/google'. Instantiate two module-scope loaders (next/font requires the call to be assigned to a const at module scope with literal option values):
    - plusJakartaSans: subsets ['latin'], weight ['400', '700'] (400 for card body, 700 for card titles per the UI-SPEC type scale), display 'swap', variable '--font-plus-jakarta-sans'.
    - dmSans: subsets ['latin'], weight ['700'] (CTA buttons are 700 only per the UI-SPEC), display 'swap', variable '--font-dm-sans'.
    Export both consts as named exports (export const plusJakartaSans = ...; export const dmSans = ...). The variable strings must be exactly --font-plus-jakarta-sans and --font-dm-sans so they match the --font-card / --font-cta registrations in globals.css (plan 04-01). Do NOT import these fonts via a CSS @import in globals.css — the whole point of next/font is self-hosted, optimized, layout-shift-free loading.
  </action>
  <verify>
    <automated>grep -q "Plus_Jakarta_Sans" src/lib/fonts.ts && grep -q "DM_Sans" src/lib/fonts.ts && grep -q -- "--font-plus-jakarta-sans" src/lib/fonts.ts && grep -q -- "--font-dm-sans" src/lib/fonts.ts && npx tsc --noEmit -p tsconfig.json && echo FONTS_TS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/lib/fonts.ts imports Plus_Jakarta_Sans and DM_Sans from 'next/font/google'.
    - Source assertion: plusJakartaSans declares variable '--font-plus-jakarta-sans' with weights 400 and 700; dmSans declares variable '--font-dm-sans' with weight 700.
    - Source assertion: both loaders are exported as named consts.
    - Test command: `npx tsc --noEmit` passes (no type errors from the new module).
  </acceptance_criteria>
  <done>src/lib/fonts.ts exists, exports plusJakartaSans and dmSans with the exact CSS-variable names, and type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the font variables to the storefront root layout</name>
  <files>src/app/[locale]/layout.tsx</files>
  <read_first>
    - src/app/[locale]/layout.tsx (current <html lang={locale} suppressHydrationWarning> element, line ~77)
    - src/lib/fonts.ts (the exports created in Task 1 — plusJakartaSans, dmSans)
  </read_first>
  <action>
    In src/app/[locale]/layout.tsx, import { plusJakartaSans, dmSans } from '@/lib/fonts' (absolute @/ import per CLAUDE.md). Add a className to the existing <html lang={locale} suppressHydrationWarning> element that includes both font variable classes: className={`${plusJakartaSans.variable} ${dmSans.variable}`}. Keep lang and suppressHydrationWarning intact. Do NOT change the <body> className (it keeps antialiased bg-background text-foreground) and do NOT alter the default body font — next/font here only defines CSS variables; existing storefront text keeps rendering with --font-body/--font-heading. Only the storefront [locale] tree is touched, so admin/ingredients layouts (separate <html> roots) remain unaffected, preserving the out-of-scope contract.
  </action>
  <verify>
    <automated>grep -q "plusJakartaSans" src/app/[locale]/layout.tsx && grep -q "dmSans" src/app/[locale]/layout.tsx && grep -q "@/lib/fonts" src/app/[locale]/layout.tsx && npx next build 2>&1 | tee /tmp/next-build-04.log | grep -Eq "Compiled successfully|✓ Compiled" && echo LAYOUT_BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: [locale]/layout.tsx imports plusJakartaSans and dmSans from '@/lib/fonts'.
    - Source assertion: the <html> element's className includes both `${plusJakartaSans.variable}` and `${dmSans.variable}`.
    - Source assertion: <body> className and the <html> lang / suppressHydrationWarning attributes are unchanged.
    - Build command: `npx next build` compiles successfully (this is the phase-level build gate — catches any font/JSX/token error across all three plans). Per project memory, run the real Next build, not just tsc.
  </acceptance_criteria>
  <done>The storefront <html> applies both next/font variable classes; `next build` compiles successfully; admin/ingredients layouts and the storefront body font are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan adds a font-loading module and applies CSS-variable classNames in a layout. No user input, no data handling, no auth surface, no new endpoints, no schema changes. `next/font/google` is first-party Next.js API that self-hosts fonts at build time (no runtime third-party request), so no new external trust boundary is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | N/A | next/font wiring | accept | No attack surface — build-time self-hosted font optimization only. Out-of-scope layouts (admin, ingredients) use separate <html> roots and are not modified. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 04-02)

- `src/lib/fonts.ts` — new module exporting `plusJakartaSans` (variable `--font-plus-jakarta-sans`, weights 400/700) and `dmSans` (variable `--font-dm-sans`, weight 700) via `next/font/google`.
- CSS variables `--font-plus-jakarta-sans` and `--font-dm-sans` — provided at runtime on the storefront `<html>` element; consumed by plan 04-01's `--font-card` / `--font-cta` `@theme` tokens.
- `src/app/[locale]/layout.tsx` — now applies both font variable classNames to `<html>` (additive; body font behavior unchanged).
</artifacts_produced>

<verification>
- `src/lib/fonts.ts` exists and exports the two loaders with the exact variable names `--font-plus-jakarta-sans` / `--font-dm-sans`.
- `src/app/[locale]/layout.tsx` imports from `@/lib/fonts` and applies both `.variable` classes on `<html>`.
- `npx tsc --noEmit` passes and `npx next build` compiles successfully.
- Admin and ingredients layouts, and the storefront `<body>` font, are unchanged.
</verification>

<success_criteria>
Plus Jakarta Sans and DM Sans load through next/font/google and their CSS variables are live on the storefront `<html>`, ready for `--font-card` / `--font-cta` — satisfying the font-loading half of DSGN-02 — with the build green and out-of-scope pages untouched.
</success_criteria>

<output>
Create `.planning/phases/04-design-system-foundation/04-02-SUMMARY.md` when done.
</output>
